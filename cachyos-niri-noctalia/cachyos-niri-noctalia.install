pre_upgrade() {
  echo "==> NOTICE: polkit-kde-agent is no longer included by default."
  echo "==> If necessary, explicitly reinstall 'polkit-kde-agent' or use the Noctalia polkit-agent plugin."
}

post_install() {
  post_upgrade
}
