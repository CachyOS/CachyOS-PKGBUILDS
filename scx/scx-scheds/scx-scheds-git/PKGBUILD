# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.0.20.r180.g138b95e6
pkgrel=2
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64' 'aarch64')
license=('GPL-2.0-only')
depends=(
  bash
  bpf
  gcc-libs
  glibc
  jq
  libseccomp
  libbpf
  libelf
  protobuf
  scx-tools
  zlib
)
makedepends=(
  clang
  git
  llvm
  llvm-libs
  python
  rust
)
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')
options=(!lto)
provides=("scx-scheds=$pkgver")
conflicts=("scx-scheds")
replaces=('scx-cake')

_backports=(
2765b530e86682c27a46c2821df8d2640e3f0a86 # scx_cake: dead code cleanup + p->scx.slice locality promotion
d0dc9032382c633645508847e6091e79f869adba # scx_cake: eliminate bpf_task_storage_get from enqueue via dsq_vtime staging
315670be8f67b060d36382f1f76cbbe8b7d78b5b # scx_cake: eliminate bpf_task_storage_get from running and tick via mailbox staging
e994258eedbd036fb3cbf7d282a7575e6416614b # scx_cake: confidence-gated stopping eliminates kfunc for stable tasks
1634bfc30d20819ea271749f9da6f8c17807dd65 # scx_cake: benchmark-driven pattern optimizations for 9800X3D
91ec39a76f86bac1f7cd8c12d68265650736072e # cake: branchless arithmetic optimizations (M6+M8+R1)
68de08b917e968ccbe5e7025676479d734a7000c # fix(cake): eliminate Elden Ring tier oscillation + core migration
1be054eedc1a06903844c6cbb8cda3ee50bfa3da # chore: add mangohud/ to gitignore
ce063b982078829859571ee96257c89c2ef21110 # cake: S2 CPU placement + A4 tick-assembled tier snapshot
9ecbf1d7ddf4b57266f037ce90712823c7dc0023 # cake: fix CPU affinity crash in Gate 2 + optimize check
19339ba549581eda8161e26451327dd9de732717 # cake: upgrade to scx_bpf_locked_rq with __weak fallback
02188458e4336bdf85b081e0b15551f690509c43 # cake: HFT microarch optimizations (write coalescing + cold-path deferral)
ef824d694374c4dd662018c60087c5813240b849 # scx_cake: MONSTER optimization + select_cpu affinity fix
657bb09bc5a3955048843e4f7e5fd24d37128b9f # fix(cake): eliminate u16 counter overflow cascades
1dcb684373617d49228c1a9bfbad5546e2668b09 # fix(cake): correct mega_mailbox struct alignment
5262a84af419fd621c5f8274aeeae61a53f08150 # OPT2+OPT6: eliminate kfuncs from cake_stopping
1a8287825bc63e7178df010eec10c44763bf1e3e # OPT2/OPT6: inline cold classification + slice-delta runtime
806d6247460fea868335709b1ecbe67924db0233 # cake: Gate 4 lazy preempt — T0/T1 dispatch to worst-tier CPU via LOCAL_ON
05e2f027c1d66ca1616bee1fa839670ef1619140 # cake: R2 3-slot cache, R4 WSC hint, C1 interleaved u16, C2 deferred promotion
7fbfcf0cd7da282c3f901758dcb1e90bdc140ea3 # cake: Gate 1c migration cooldown (NEAR_PREF) + DUAL_PHASE bimodal slice boost
b9fcd1f20d6041c4c95c729d021bd8ed563efbc3 # scx_cake: detect headless terminal and skip TUI/splash
b5bf34d1a9675e9246ebd062129f877ed5eb6bcf # scx_cake: eliminate 4 sources of false sharing
4cbd3b16d074e471eb7bc201ec9db54cb4830750 # perf(cake): trampoline latency hiding — defer kfunc + pre-load + MLP
8be4ac70922dbebba31ac5d07b2c800d7e401824 # perf(cake): revert spilled pre-loads per codegen audit
7113fbd425c91e2812f620fcc0a978c70936bb68 # cake: remove dead code and add jitter guards
9e4f9ae1f99d907c16c0f19cdea79f29fcc7796b # arena: migrate per-task storage from bpf_task_storage_get to sdt_task arena
dad0bd088bd78a210fb3a6ed87d6dff928935cc5 # arena: migrate mega_mailbox + global_scratch to arena per-CPU arrays
6c6f3cd09b848ea2391c382c002d6d0efb27d619 # arena: tier-occupancy bitmask for dispatch skip optimization
d6c37c60d67cb5c14498195637a3fcc90c79ff27 # arena: optimize ARENA_ASSOC to inline asm (2 insns vs 3)
39dc63efd1886d217014a58e1cadbc64019a67ec # scx_cake: arena audit — fix tier_occupied race, stale preempt scratch
2618e40c23173f40799a82b94edec2ab943aaac2 # cake/bpf: C1 spatial consolidation — merge arena_mailbox + arena_scratch into unified per_cpu block
89a3f6dabb06eecced44e1e152e4eb781ac07998 # scx_cake: bump scx_arena version to 0.1.4
55f7bc82cbc9c1522b2071d374b8ccdad786027e # scx_cake: fix CI exit code 143, proper UEI shutdown, skip ETD headless, fix heatmap layout
4e8fca239375479f676650303490a6a4e33db33f # scx_cake: Final Jitter, Latency, and Topology Audit (Phase 3 & 4)
d7ef6b0de956475a5b57cd997697e37cee1263f0 # Phase 5: Yielder Privilege System
8731f086abf42325c2b7f174162f440dbbc652a4 # scx_cake: waker-aware CPU placement (Gate 1W) & self-scaling yield priority
b11f26b00fc74781905a6a7f5f0f3223f3952ffa # BenchLab 19-entry + Disruptor handoff: psychic cache removed, zero-arena cake_stopping
9c28c01085c8d037cb38b7d0066c6f2674e39a9a # Cargo.lock: update crates for scx_cake
89d2b139134d74329f5dca12c878860676436b97 # scx_cake: try sysinfo 0.35.2
0e16267097ee53de8b3a712ccef5c10296b13eaa # perf: execution order optimization + data flow audit
efae218d7026b5581d742ced5d0b6edd28f99b77 # cake: waker priority inheritance + BenchLab overhaul
9a4de8447978a1d700d74d463e8364a501cf33cc # cake: benchlab scrollable table + select_cpu_dfl + kick_cpu benchmarks
8b5a77c8eabe5fb254cf3a93f2e78968712718b0 # cake: fix runtime crash — remove context-restricted kfunc benches
5d753c62791d94a9fe50120776b7e515aa174f9b # perf: gate architecture restructure + zero-arena running + idle shadow hints
4f49b525e044d529341aae9d6cb3b0a00c486d46 # cake: waker-chain gate + variable deduplication
b7458fac9c603c5042569cd2a11e0cc24d290226 # v1.0.3-Alpha: PPID-based game family detection
4301eb3d0cce1d8b44df6e7ee48629d3b3c5e16b # cake: add 15s hysteresis to game detection to prevent alt-tab freeze
23e9951dd2061bd5326ddc20396af4fa4b1f1c65 # fix(scx_cake): PPID game family detection broken in release builds
13a6b490b8e39bb42a39d16c0d3bb0035d64de21 # fix: eliminate TOCTOU race in CAKE_STATS_ACTIVE causing callback profile corruption
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --release \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scx_mitosis \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/release \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
