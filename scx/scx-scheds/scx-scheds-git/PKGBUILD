# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.0.20.r252.gec3423bc
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64' 'aarch64')
license=('GPL-2.0-only')
depends=(
  bash
  bpf
  gcc-libs
  glibc
  jq
  libseccomp
  libbpf
  libelf
  protobuf
  scx-tools
  zlib
)
makedepends=(
  clang
  git
  llvm
  llvm-libs
  python
  rust
)
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')
options=(!lto)
provides=("scx-scheds=$pkgver")
conflicts=("scx-scheds")
replaces=('scx-cake')

_backports=(
7514e70f83f95b778ab280e2ea26dbb96e229985 # chore(scx_cake): fix Cargo.toml
5face68425d7efda66792898ba3b87f246aa4d44 # scx_cake: dead code cleanup + p->scx.slice locality promotion
885110d411d479d57e4cdc202c354cb82396e186 # scx_cake: eliminate bpf_task_storage_get from enqueue via dsq_vtime staging
5ed399df2e7c497cb21d3598ba04796b930fd7df # scx_cake: eliminate bpf_task_storage_get from running and tick via mailbox staging
66fa0c7dcbc982382faad36d3091eb52ece4278a # scx_cake: confidence-gated stopping eliminates kfunc for stable tasks
2ad6adafe212f05699c33e0980b91e9a3de06335 # scx_cake: benchmark-driven pattern optimizations for 9800X3D
3facbc98860058e9cc66c8fdb6d11151445da08d # cake: branchless arithmetic optimizations (M6+M8+R1)
d31a5d469db20ad5f5fdf8c69681eb95d6bb0431 # fix(cake): eliminate Elden Ring tier oscillation + core migration
935ac00a7b965590795f93c86cf2ec97e25a5b49 # chore: add mangohud/ to gitignore
7b75f4f1a37736e9b8a0c11ec56fb1f2e73a3415 # cake: S2 CPU placement + A4 tick-assembled tier snapshot
ed3edf2851be9c718ae1588cef90fb57a2fc01be # cake: fix CPU affinity crash in Gate 2 + optimize check
a26b802492e4a080b74695d1bc4eb7a747d8aab6 # cake: upgrade to scx_bpf_locked_rq with __weak fallback
19c222d2a0ac8445a454a4254eb88456a1d91ff8 # cake: HFT microarch optimizations (write coalescing + cold-path deferral)
c789895659cb52cc2d729ae092454f5e49a11584 # scx_cake: MONSTER optimization + select_cpu affinity fix
a7b9b604a2133ecde86dd682cebe89c208f0d6cf # fix(cake): eliminate u16 counter overflow cascades
fab5a4eaabe90e491657062af51940e069ef2db1 # fix(cake): correct mega_mailbox struct alignment
f14ae8fad5655dbdfb3665f5c12de83b682881fd # OPT2+OPT6: eliminate kfuncs from cake_stopping
e5c2391bed3a82ffaf2b9ebb56e95251a9477b26 # OPT2/OPT6: inline cold classification + slice-delta runtime
5d6d0f37247083656a584c2465f0f63d970b1271 # cake: Gate 4 lazy preempt — T0/T1 dispatch to worst-tier CPU via LOCAL_ON
51cb212caa838e53133077a9ca4c38d9cbd21dec # cake: R2 3-slot cache, R4 WSC hint, C1 interleaved u16, C2 deferred promotion
4c1871ece9d956dd2ee64a8f28666d09a2b70376 # cake: Gate 1c migration cooldown (NEAR_PREF) + DUAL_PHASE bimodal slice boost
a3f71db23062f9e29214432e8c21dd3434e23f4a # scx_cake: detect headless terminal and skip TUI/splash
b0c5b919f063ddd7db23b415a4f2656d18735e7d # scx_cake: eliminate 4 sources of false sharing
0cd8722abd3cfc9a8afa166b10ec1eba9cc79174 # perf(cake): trampoline latency hiding — defer kfunc + pre-load + MLP
bd388b206e05737272a1ce1270de51d328498959 # perf(cake): revert spilled pre-loads per codegen audit
9c0615d41c6a0425ce3ee4cc696ce6cdb6b372e1 # cake: remove dead code and add jitter guards
44a3c02b43a71d4b7246b3db36e2cc922ec590d2 # arena: migrate per-task storage from bpf_task_storage_get to sdt_task arena
9eea0bbaddd695ab4851ae20ab15b171b49cbda9 # arena: migrate mega_mailbox + global_scratch to arena per-CPU arrays
6999e60a74f530408c8ae0e66b1398e8f59cfb20 # arena: tier-occupancy bitmask for dispatch skip optimization
56a573be33e4600c88afceb19cbe0d4fef305faf # arena: optimize ARENA_ASSOC to inline asm (2 insns vs 3)
d933120e5e176b268c63032d4ccc07b65a4dabbd # scx_cake: arena audit — fix tier_occupied race, stale preempt scratch
5ee7ab1ab915d02f9f79b50d33d96da5958127aa # cake/bpf: C1 spatial consolidation — merge arena_mailbox + arena_scratch into unified per_cpu block
1e73b04222fe0c4724671e891bad7f93ce245b12 # scx_cake: bump scx_arena version to 0.1.4
165928a9f7d7f2e510b2ae8ea750b607f4c22f82 # scx_cake: fix CI exit code 143, proper UEI shutdown, skip ETD headless, fix heatmap layout
81d5e152b01af366796864ba418cefb86dce976a # scx_cake: Final Jitter, Latency, and Topology Audit (Phase 3 & 4)
f5c64d04e2e52e735457139ef0359880b9021c2d # Phase 5: Yielder Privilege System
b0805882c4d574c7840fc41cd9842171048dd5e9 # scx_cake: waker-aware CPU placement (Gate 1W) & self-scaling yield priority
4f29c75ab173b589d533e5aaf812e250d8ce9925 # BenchLab 19-entry + Disruptor handoff: psychic cache removed, zero-arena cake_stopping
e620e897b086a399e923071ba79019e3f38eb354 # scx_cake: fix PR import issues (restore rustland_core include, sysinfo 0.35.2)
0bf82fa6074002bcef9ae4c9d070d5f18e2cfb78 # perf: execution order optimization + data flow audit
e1b25c89b91b3e2ccbb01ed9a68244c802dc4302 # cake: waker priority inheritance + BenchLab overhaul
cb1b1796f2ad76abf9557cf75c80986df4d70ed1 # cake: benchlab scrollable table + select_cpu_dfl + kick_cpu benchmarks
464af9fb95c24d54b6490d05a55050a6e1973495 # cake: fix runtime crash — remove context-restricted kfunc benches
fc6e4b18389da00a4b81a598c9d25e0ad99fe8e6 # perf: gate architecture restructure + zero-arena running + idle shadow hints
02c7dae392d5cc13cfab8cb500fdcff637ff6c2f # cake: waker-chain gate + variable deduplication
d6f0d48aa9ee3e0902f09ee0dc0df0972dd6d226 # v1.0.3-Alpha: PPID-based game family detection
a1ce4d1a0213d3ec0006e82fb4d218e4019fddd0 # cake: add 15s hysteresis to game detection to prevent alt-tab freeze
2135d09ee6d5f06a078bcb3abac175097c268dbe # fix(scx_cake): PPID game family detection broken in release builds
4bb945e60425486ff7998b80519ccb280b1b8fc3 # fix: eliminate TOCTOU race in CAKE_STATS_ACTIVE causing callback profile corruption
dc15a24ca0e999b26035134bcccbea39c6b484cc # scx_cake: clock domain unification, Gate 1S removal, adaptive preemption
0594da30e19af2499305a55f549b4fcb26e07184 # scx_cake: update Cargo.lock
f28c76439c45235c0fd8ea498dd55c6a58e2367d # scx_cake 1.0.4: Storage consolidation + fast paths
79fbdd14f8f7a4a9ffc845b93a6f77ba27815229 # scx_cake: simplify passes — GAME throughput, logic flow, migration reduction
b028bea7f3c49ab006d392cfee3d00c068b5a03a # scx_cake: COMPILATION throughput + GAMING unfairness + verifier fix
d9a83c4ee7fff3e1b2aa27cc02e7c88f90ed44eb # scx_cake: release build correctness fixes
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --release \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scx_mitosis \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/release \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
