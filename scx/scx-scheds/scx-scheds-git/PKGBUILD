# Maintainer: Peter Jung ptr1337 <admin@ptr1337.dev>
# Maintainer: Piotr Górski <lucjan.lucjanov@gmail.com>

pkgname=scx-scheds-git
_gitname=scx
pkgver=1.0.20.r238.g6fa5e78c
pkgrel=1
pkgdesc='sched_ext schedulers and tools'
url='https://github.com/sched-ext/scx'
arch=('x86_64' 'aarch64')
license=('GPL-2.0-only')
depends=(
  bash
  bpf
  gcc-libs
  glibc
  jq
  libseccomp
  libbpf
  libelf
  protobuf
  scx-tools
  zlib
)
makedepends=(
  clang
  git
  llvm
  llvm-libs
  python
  rust
)
source=("git+https://github.com/sched-ext/scx")
sha256sums=('SKIP')
options=(!lto)
provides=("scx-scheds=$pkgver")
conflicts=("scx-scheds")
replaces=('scx-cake')

_backports=(
fe3b86bf38873fb47dbfe3cd009845dce3e35b2e # chore(scx_cake): fix Cargo.toml
a7d1b056211b5e6bcac17b5fe47b6ca9f1beebe3 # scx_cake: dead code cleanup + p->scx.slice locality promotion
bdf9b8ec0e08beba267e4b1562b395dc8bd595d5 # scx_cake: eliminate bpf_task_storage_get from enqueue via dsq_vtime staging
dc15570de508e832278c1556958d477dccd1bf45 # scx_cake: eliminate bpf_task_storage_get from running and tick via mailbox staging
7b8c753ccdebcf5cd0f7d52f0e8d8fd218b1c549 # scx_cake: confidence-gated stopping eliminates kfunc for stable tasks
300e94de0bfef10e5b8df150b7047e9fe09231f1 # scx_cake: benchmark-driven pattern optimizations for 9800X3D
b38aba73cfff294a3484ad08661d2b1f95b6c381 # cake: branchless arithmetic optimizations (M6+M8+R1)
464dba45c862379af6b974cab61cf6803b5a36dd # fix(cake): eliminate Elden Ring tier oscillation + core migration
a415889985d7b91f8d9e1d1c3e747fba67329c5b # chore: add mangohud/ to gitignore
e80ac92dfce1e8a890c0036dcf95647bf0ffa8fb # cake: S2 CPU placement + A4 tick-assembled tier snapshot
34c69c684ca2d219da6db0174e198e35e4d795ce # cake: fix CPU affinity crash in Gate 2 + optimize check
0c5303e7a99eebd9206d96f12bf8946ae04533c2 # cake: upgrade to scx_bpf_locked_rq with __weak fallback
80c801947ddb167cf9eb0be7a4f3d3755b09fe45 # cake: HFT microarch optimizations (write coalescing + cold-path deferral)
42b37cb144d0b9ad2681704cdcc86ae23dda5ed4 # scx_cake: MONSTER optimization + select_cpu affinity fix
adc07c3686b3f69a88c373d20b983d402e67070c # fix(cake): eliminate u16 counter overflow cascades
d44ecc73bb093d4b524e053c18e0c770417dd2d2 # fix(cake): correct mega_mailbox struct alignment
5c7919dc5160b4667b7b890671e507469cf14c35 # OPT2+OPT6: eliminate kfuncs from cake_stopping
dc757b3d549e4644c5e8c9ead6e9d7efdaf7dd8a # OPT2/OPT6: inline cold classification + slice-delta runtime
5c3f44077f10aa5c6f757464490d8db946f66593 # cake: Gate 4 lazy preempt — T0/T1 dispatch to worst-tier CPU via LOCAL_ON
51e2486d5a479e2243b0554806f1a87e19018b2c # cake: R2 3-slot cache, R4 WSC hint, C1 interleaved u16, C2 deferred promotion
5ceda23b0dba3ee119526fc305e615228ff9b08b # cake: Gate 1c migration cooldown (NEAR_PREF) + DUAL_PHASE bimodal slice boost
198ada37cb7a5bcf360ab99ee4a90d39f406558b # scx_cake: detect headless terminal and skip TUI/splash
7524b6635f47b02411579e7da487731258f5d326 # scx_cake: eliminate 4 sources of false sharing
f4dc7a2a03acb5c2a09bd0bdea8de2bcdeac756c # perf(cake): trampoline latency hiding — defer kfunc + pre-load + MLP
2faeb9af127e471525f96dccd6abe1783c860e69 # perf(cake): revert spilled pre-loads per codegen audit
7f3c6d5332064f42a87a0ba573eded6fd34541f9 # cake: remove dead code and add jitter guards
365118c9a795ecb053d269c50c2b76a794127cf7 # arena: migrate per-task storage from bpf_task_storage_get to sdt_task arena
4522edaa840780fbe3fc9a23072d5b2211aa5bee # arena: migrate mega_mailbox + global_scratch to arena per-CPU arrays
94259fc6cd8dcd26827ed18346477fc308acdd59 # arena: tier-occupancy bitmask for dispatch skip optimization
1f53bd275e10c93613ce324f2b3d836099a9bc42 # arena: optimize ARENA_ASSOC to inline asm (2 insns vs 3)
dab8a3f7d27b18c98ddc51a520ccd806e15e458f # scx_cake: arena audit — fix tier_occupied race, stale preempt scratch
62e1ff7895fbaafcc9d2c5741c1c0e840ed7f843 # cake/bpf: C1 spatial consolidation — merge arena_mailbox + arena_scratch into unified per_cpu block
19e00ce397b80466d85027e6ff4d6d50e5d540ca # scx_cake: bump scx_arena version to 0.1.4
24c35a133d1e103c9f1caf35be10e99b3f3afa92 # scx_cake: fix CI exit code 143, proper UEI shutdown, skip ETD headless, fix heatmap layout
9bfb2dd767b4b0c89fc80a8bae7baf9cc3a1792d # scx_cake: Final Jitter, Latency, and Topology Audit (Phase 3 & 4)
1aa1f549f17c90aef0bd6ea10111dba324055a3a # Phase 5: Yielder Privilege System
08c4e4416391ac9d72c8e93ce33840cbc5a78c91 # scx_cake: waker-aware CPU placement (Gate 1W) & self-scaling yield priority
4cf8e687136eb623f2191c4129e0a3b3b18ac593 # BenchLab 19-entry + Disruptor handoff: psychic cache removed, zero-arena cake_stopping
0fd707797531dbbed437e54b189f7afae95bada7 # scx_cake: fix PR import issues (restore rustland_core include, sysinfo 0.35.2)
b652d491dae67bf17744760f6accc63aa173971a # perf: execution order optimization + data flow audit
21ecb16e3769244ba75b1fe5abbb26920aca3e1d # cake: waker priority inheritance + BenchLab overhaul
57283c7d0b48e9bf556b9b237c2fb06186b9e544 # cake: benchlab scrollable table + select_cpu_dfl + kick_cpu benchmarks
0b3c6e8f7c7ef68c6123f06959412f8ad4e3b79b # cake: fix runtime crash — remove context-restricted kfunc benches
033293dda1ca03a7bf06b09f7cd25673edc9e7b6 # perf: gate architecture restructure + zero-arena running + idle shadow hints
a2a51fcd49b7eeb340c9a038d792ff1ccb95f34a # cake: waker-chain gate + variable deduplication
1066e0563c6bc15e0d159e202328766c67282568 # v1.0.3-Alpha: PPID-based game family detection
57ba03f5e2c4ed878bbd29a3ded7b75d4ed506e0 # cake: add 15s hysteresis to game detection to prevent alt-tab freeze
7e47b46bb1cf02d912d84f51777e9423e2b50f2b # fix(scx_cake): PPID game family detection broken in release builds
c16b27ab3085579c58e0cd6edde8d10336bebf4d # fix: eliminate TOCTOU race in CAKE_STATS_ACTIVE causing callback profile corruption
3bc9e835d13e71fe38dbfe734732fe86f1ea561c # scx_cake: clock domain unification, Gate 1S removal, adaptive preemption
16665ea7a0eacc97b54d597de26b7a0c76debdd5 # scx_cake: update Cargo.lock
1f53862e3612d2a11c679ee69d897c6a54af1a36 # scx_cake 1.0.4: Storage consolidation + fast paths
9868cdedfe6806541e719ed2abf4255cad03ac6f # scx_cake: simplify passes — GAME throughput, logic flow, migration reduction
9d84b1c06269fcf10664b606538dd7fe3215ded8 # scx_cake: COMPILATION throughput + GAMING unfairness + verifier fix
c984cd8e84245241fb939367f78ad01f3d7d6798 # scx_cake: release build correctness fixes
)

_reverts=(
)

pkgver() {
  cd $_gitname
  git describe --long --tags | sed 's/^v//;s/\([^-]*-g\)/r\1/;s/-/./g'
}

prepare() {
  cd $_gitname

  local _c _l
   for _c in "${_backports[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git cherry-pick --mainline 1 --no-commit "${_c}"
   done
   for _c in "${_reverts[@]}"; do
     if [[ "${_c}" == *..* ]]; then _l='--reverse'; else _l='--max-count=1'; fi
     git log --oneline "${_l}" "${_c}"
     git revert --mainline 1 --no-commit "${_c}"
   done

  local src
   for src in "${source[@]}"; do
     src="${src%%::*}"
     src="${src##*/}"
     [[ $src = *.patch ]] || continue
     echo "Applying patch $src..."
     patch -Np1 < "../$src"
   done

  export RUSTUP_TOOLCHAIN=stable
  cargo fetch --locked --target "$(rustc -vV | sed -n 's/host: //p')"
}

build() {
  cd $_gitname
  export RUSTUP_TOOLCHAIN=stable
  export CARGO_TARGET_DIR=target
  cargo build \
     --release \
     --frozen \
     --all-features \
     --workspace \
     --exclude scx_rlfifo \
     --exclude scx_wd40 \
     --exclude scx_mitosis \
     --exclude scxcash \
     --exclude xtask \
     --exclude vmlinux_docify \
     --exclude scx_arena_selftests
}

package() {
  cd $_gitname

  # Install all built executables (skip .so and .d files)
  find target/release \
    -maxdepth 1 -type f -executable ! -name '*.so' \
    -exec install -Dm755 -t "$pkgdir/usr/bin/" {} +
}
