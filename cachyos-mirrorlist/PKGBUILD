
pkgname=cachyos-mirrorlist
pkgver=6
pkgrel=1
pkgdesc="cachyos mirrorlist"
arch=('any')
license=('GPL3')
groups=(cachyos)
options=(!strip !emptydirs)
backup=("etc/pacman.d/$pkgname")  # to keep possibly ranked mirrorlist
source=(cachyos-mirrorlist)
sha512sums=('df3b0999a377d507cbde85526771719dfa2eb0142972798d9467d95acf99f706c3cea39145d7f2cdad95ccea30fc276de561c653f2bba32d32d0f60bad92ceba')
package() {
 #install -d "$pkgdir/etc/pacman.d"
 install -Dm644 "$srcdir/$pkgname" "$pkgdir/etc/pacman.d/$pkgname"
}
