# Maintainer: Vladislav Nepogodin <nepogodin.vlad@gmail.com>

pkgname=cachyos-kde-settings
pkgdesc='CachyOS KDE settings'
pkgver=3.0
pkgrel=1
arch=('any')
url="https://github.com/cachyos/$pkgname"
license=('GPL')
makedepends=('coreutils')
source=("git+https://github.com/CachyOS/$pkgname.git#branch=rework")
sha512sums=('SKIP')
depends=('ttf-fantasque-nerd'
         'noto-fonts'
         'ttf-fira-sans'
         'capitaine-cursors')
install=$pkgname.install
provides=('cachyos-desktop-settings')
conflicts=('cachyos-desktop-settings')

package() {
    install -d $pkgdir/etc
    cp -rf $srcdir/$pkgname/etc $pkgdir
    install -d $pkgdir/usr
    cp -rf $srcdir/$pkgname/usr $pkgdir
}
