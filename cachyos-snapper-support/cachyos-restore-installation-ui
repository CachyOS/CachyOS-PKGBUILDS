#!/bin/bash

HELPER=/usr/lib/cachyos/restore-installation
TITLE="Restore Fresh Installation"
# ANSI-C quoting so real newlines reach kdialog/zenity instead of literal \n.
CONFIRM_MSG=$'This will roll back the system to the original CachyOS installation snapshot.\n\nFiles in /home (including Documents, Pictures, etc.) are preserved.\nYour ~/.config will be renamed to ~/.config.bak.<timestamp> so the restored defaults take effect.\n\nThe change applies on the next reboot. Continue?'
SUCCESS_MSG=$'Rollback complete.\n\nPlease reboot your computer to boot into the restored installation.'
FAIL_MSG="Restore failed. Your system has not been rolled back."

# Pick a GUI frontend once, so confirmation, success and error dialogs
# all use the same tool.
if command -v kdialog > /dev/null 2>&1; then
    GUI=kdialog
elif command -v zenity > /dev/null 2>&1; then
    GUI=zenity
else
    echo "No GUI dialog tool found (kdialog or zenity required)." >&2
    exit 1
fi

ask() {
    case "$GUI" in
        kdialog) kdialog --title "$TITLE" --warningyesno "$1" ;;
        zenity)  zenity --question --title="$TITLE" --text="$1" --width=400 ;;
    esac
}

info() {
    case "$GUI" in
        kdialog) kdialog --title "$TITLE" --msgbox "$1" ;;
        zenity)  zenity --info --title="$TITLE" --text="$1" --width=400 ;;
    esac
}

error() {
    case "$GUI" in
        kdialog) kdialog --title "$TITLE" --error "$1" ;;
        zenity)  zenity --error --title="$TITLE" --text="$1" --width=400 ;;
    esac
}

ask "$CONFIRM_MSG" || exit 0

if ! pkexec "$HELPER"; then
    error "$FAIL_MSG"
    exit 1
fi

info "$SUCCESS_MSG"
