# Maintainer: Peter Jung <admin@ptr1337.dev>

pkgname=cachyos-settings
pkgver=1
pkgrel=9
arch=('any')
license=('GPL')
makedepends=('coreutils')
pkgdesc='CachyOS - Settings'
source=("git+https://gitlab.com/cachyos/cachyos-settings.git")
install=$pkgname.install
sha256sums=('SKIP')
depends=(
    'irqbalance'
    'reflector'
    'haveged'
    'profile-sync-daemon'
    'zramd'
    'ananicy-cpp'
    'ananicy-rules-git'
    )
package() {
    install -d $pkgdir/$pkgname/etc
    install -d $pkgdir/$pkgname/usr

    cp -rf $pkgname/etc $pkgdir
    cp -rf $pkgname/usr $pkgdir

}
